import React from 'react';

const ToGoForm = () => {
    return (
        <form>
            <input type="number" placeholder="to go"/>
            <button>Change</button>
        </form>
    );
};

export default ToGoForm;